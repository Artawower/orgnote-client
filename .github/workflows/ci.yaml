name: CI orgnote client

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
        description: 'Environment name (dev or deploy)'
      image_tag:
        required: true
        type: string
        description: 'Docker image tag (dev or latest)'
      trigger_event:
        required: false
        type: string
        default: ''
        description: 'Event type to trigger backend deployment'

jobs:
  install_dependencies:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest
      - uses: actions/cache@v4
        with:
          path: '**/node_modules'
          key: ${{ runner.os }}-modules-${{ hashFiles('**/bun.lock') }}
      - name: Install dependencies
        run: bun install --frozen-lockfile

  lint:
    name: Run linters
    runs-on: ubuntu-latest
    needs: [install_dependencies]
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest
      - uses: actions/cache@v4
        with:
          path: '**/node_modules'
          key: ${{ runner.os }}-modules-${{ hashFiles('**/bun.lock') }}
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Prepare Quasar
        run: bunx quasar prepare
      - name: Run linters
        run: bun run lint

  tests:
    name: Run tests
    runs-on: ubuntu-latest
    needs: [install_dependencies]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest
      - uses: actions/cache@v4
        with:
          path: '**/node_modules'
          key: ${{ runner.os }}-modules-${{ hashFiles('**/bun.lock') }}
      - name: Create dummy certs for Quasar config
        run: |
          mkdir -p .certs
          openssl req -x509 -newkey rsa:2048 -keyout .certs/key.pem -out .certs/cert.pem -days 1 -nodes -subj "/CN=localhost"
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Prepare Quasar
        run: bunx quasar prepare
      - name: Run tests
        run: bun run test:ci

  push_image:
    name: Push Docker image
    runs-on: ubuntu-latest
    needs: [tests, lint]
    environment: ${{ inputs.environment }}
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest
      - name: Install dependencies
        run: |
          bun install --frozen-lockfile
          bun add global @quasar/cli
      - name: Init environment
        run: |
          cat <<EOT > .env.build
          DEBUG=${{ inputs.environment == 'dev' && 'true' || 'false' }}
          VITE_API_URL=${{ secrets.API_URL }}
          VITE_AUTH_URL=${{ secrets.AUTH_URL }}
          VITE_WS_URL=${{ secrets.WS_URL }}
          VITE_SOURCEMAP=${{ inputs.environment == 'dev' && 'true' || 'false' }}
          EOT
      - name: Build ssr
        run: bunx env-cmd -f ./.env.build bun run build -m ssr
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: deploy/Dockerfile.ssr
          push: true
          tags: orgnote/client:${{ inputs.image_tag }}

  trigger_backend:
    runs-on: ubuntu-latest
    needs: [push_image]
    if: ${{ inputs.trigger_event != '' }}
    environment: ${{ inputs.environment }}
    steps:
      - name: Trigger backend deployment
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.PAT }}
          repository: artawower/orgnote-backend
          event-type: ${{ inputs.trigger_event }}
